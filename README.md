# BillingAPP

/*install the requirements:*/
1."pip install req_install.py"
/*run this comand in the terminal*/

2.then open BillingApp.bat in editor 

3.chande the path as appropriate path of photo_billing_app.py

4.then click on "billingApp.bat"

""" the app will start run"""
